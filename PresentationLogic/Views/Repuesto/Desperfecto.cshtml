@model IEnumerable<PresentationLogic.Models.Repuesto>

@{
    ViewBag.Title = "Desperfecto";
}

<h2>Desperfecto</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdRepuesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Precio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdRepuesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Precio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.IdRepuesto }) |
            @Html.ActionLink("Details", "Details", new { id = item.IdRepuesto }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IdRepuesto })
        </td>
    </tr>
}

</table>
